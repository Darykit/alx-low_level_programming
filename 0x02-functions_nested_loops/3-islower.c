#include "main.h"

/**
 * _islower - function that checks for lowercase
 * @c: character to check
 * Return: 0 0r 1
 */

int _islower(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
	return (0);
	}
	else
	{
	return (1);
	}
}

